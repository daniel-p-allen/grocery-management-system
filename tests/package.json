{
  "name": "grocery-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Black-box tests: the services are started as real processes and driven from the outside, so nothing under src/ has to change to make them testable.",
  "scripts": {
    "test": "node --test --test-concurrency=1"
  },
  "author": "Daniel Allen",
  "license": "MIT",
  "devDependencies": {
    "mongodb": "^6.9.0",
    "mongodb-memory-server": "^10.1.2"
  }
}
